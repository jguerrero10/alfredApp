"""App configuration for the Drivers application."""

from django.apps import AppConfig


class DriversConfig(AppConfig):
    """Configuration class for the Drivers app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "drivers"
