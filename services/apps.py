"""Apps module for the Services application."""

from django.apps import AppConfig


class ServicesConfig(AppConfig):
    """Configuration class for the Services app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "services"
